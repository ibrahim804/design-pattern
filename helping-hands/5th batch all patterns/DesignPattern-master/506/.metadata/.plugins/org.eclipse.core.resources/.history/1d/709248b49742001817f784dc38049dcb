package classwork;

public class Transaction {
	
	private int balance;
	
	public void deposit(int amount) {
		if(amount > 0)
			balance += amount;
	}
	
	public void deposit(int amount) {
		if(amount < 0)
			balance += amount;
	}
	
}
